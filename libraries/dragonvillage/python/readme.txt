- convert_utf8.py : "../src"하위 경로의 모든 *.lua파일들을 UTF-8인코딩으로 변경해주는 python 파일

- xor.py : "../src"하위 경로의 모든 *.lua파일들을 xor 암호화해주는 python 파일 (개발 중)

- xor_data.py : "../data"하위 경로의 모든 *.csv, *.txt파일들을 xor 암호화해주는 python 파일 (개발 중)

- src_to_emulator.py : 개발 폴더에서 리소스 폴더로 lua파일을 복사하는 python 파일, 경로는 환경변수 사용

- change_ext.py : 확장자 변경하는 util